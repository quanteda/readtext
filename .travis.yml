r_packages:
    - covr
language: r
sudo: true
dist: trusty
before_install:
  - sudo apt-get install libapparmor-dev
env:
    global:
    - R_CHECK_ARGS="--no-build-vignettes"
after_success:
  - Rscript -e 'library(covr); codecov()'
